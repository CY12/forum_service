<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.forum.mapper.WeatherCommonsMapper">

    <select id="getWeatherCommons" parameterType="map" resultType="com.example.forum.entity.WeatherCommons">
        SELECT title,url FROM `weather_commons` order by updated_time
    </select>
</mapper>